import random         ->importing random module

from random import choice  ->another way of importing

random.randint(1, 10) ->it will generate random integer including  1, 10

random.random()       ->for generating random values

random.seed()         ->we can give our own value in seed by which randit and random can generate  a new value

random.choice()       ->select random data from the list

random.shuffle()      ->shuffles item in the list

-----------------

import math            ->module

math.ceil()            ->returning integer  ->math.ceil(13.1) ->13
                                            ->math.ceil(101.96)-> 102
